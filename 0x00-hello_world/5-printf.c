#include <stdio.h>
/**
 * main - This exercise prints a message using command print
 *
 * Return: Always 0
 */
int main(void)
{
printf("\"with proper grammar, but the outcome is a piece of art,");
return (0);
}
